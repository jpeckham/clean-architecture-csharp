name: Pull Request Validation

on:
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  validate:
    name: Build and test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.x

      - name: Restore
        run: dotnet restore SocialApp.sln

      - name: Build
        run: dotnet build SocialApp.sln --configuration Release --no-restore

      - name: Test
        run: dotnet test SocialApp.sln --configuration Release --no-build

      - name: Docker Compose config
        run: docker compose config
